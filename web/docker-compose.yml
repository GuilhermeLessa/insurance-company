services:

  web:
    container_name: insurance-web
    build:
      context: ../web
      dockerfile: Dockerfile
    restart: always
    tty: true
    ports:
      - '5173:5173'
    networks:
      - web-insurance-network

networks:
  web-insurance-network:
    external: 
      name: insurance-network